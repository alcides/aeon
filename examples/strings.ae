import prelude.J
import prelude.S

main : (args:Array<String>) -> _:Void {

    J.out("teste1")

    a : String = "ola"
    b : String = "adeus"

    c : String = S.concat(a, b)

    J.out(c)
    J.out(S.size(c))
}