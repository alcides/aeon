import ..prelude.J
import ..prelude.A
import ..prelude.R


arraySum : (ar:A.Array<Integer>) -> i:Integer {
  A.reduce(ar, (i:Integer, j:Integer) -> i + j)
}

main : (args:Array<String>) -> _:Void {
   e : A.Array<Integer> = A.range(0, 1000)
   J.out(arraySum(e))
}
