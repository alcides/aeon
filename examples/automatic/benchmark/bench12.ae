import aeon/libraries/list;

// Last Index of Zero:
// -------------------
// Given a vector of integers, at least one of which is 0, return
// the index of the last occurrence of 0 in the vector
last_index_zero(l:List<Integer>) -> out:Integer where {out >= 0 && out < l.size and
                                                       out == index(0, reversed(l))} {
    //index(0, reverse(l));
    ??;
}