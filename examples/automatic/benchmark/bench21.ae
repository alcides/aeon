import aeon/libraries/list;

// Negative To Zero:
// -----------------
// Given a vector of integers, return the vector where all negative
// integers have been replaced by 0.
negative_to_zero(l1:List<Integer>) -> l2:List<Integer> where {l1.size == l2.size and 
                                                              count[Integer](0, l2) == length[Integer](filter[Integer](\x:Integer -> x <= 0, l1))} {
    ??;
}