import aeon/libraries/list;

// Count Odds:
// -----------
// Given a vector of integers, return the number of integers that
// are odd, without use of a specific even or odd instruction
count_odds(l:List<Integer>) -> out:Integer where {out >= 0 && out < l.size and
                                                  out == length(filter(\x:Integer -> x % 2 == 0, l)}) {
    // length(filter(\x:Integer -> x % 2 == 0, l));
    ??;
} 