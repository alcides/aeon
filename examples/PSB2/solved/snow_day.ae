def isZero : (n: Int) -> Bool = \n -> n == 0;

def snow_day ( n: Int) ( m: Float) (t: Float) (p: Float) : Float {
    if isZero n then
        m
    else
        a: Float = m +. t;
        b: Float = m *. p;
        c: Float = a -. b;
        n1: Int = n - 1;

        snow_day n1 c t p

}

def main(n: Int) : Unit{
    print (snow_day 10 0.0 1.0 0.0)
}
