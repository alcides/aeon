def sqrt : (i: {x:Int | x > 0} ) -> Float := fun i => native "__import__('math').sqrt(i)"
def sqrt : (i:Int | i > 0) -> Float := fun i => native "__import__('math').sqrt(i)"

def main (i:Int) : Unit := print (sqrt 25)
