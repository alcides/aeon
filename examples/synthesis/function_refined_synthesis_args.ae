import Math (abs)

def test (j:{h:Int | h > 0 }): Int := 8

@minimize_int(Math.abs(64 - func(8)))
def func (i:{x:Int | (x > 0 && x < 10) || (x > 20 && x < 30)}) : Int  := (?hole: {g:Int | g > 0 } ) * i
