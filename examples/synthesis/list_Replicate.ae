import "List.ae";
import Math_abs from "Math.ae";

@minimize_int(Math_abs((List_sum (replicate 3 3)) - 9))
def replicate (n: {x:Int | x > 0} ) (elem: Int ) : List {
    (?hole: { j:List | n == (List_size j) })
}
