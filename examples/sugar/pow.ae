type Unit;
def math : Unit = native_import "math";
def test (b: {c:Float | ((c >= 1)  && (c <= 100))}) -> 
            (e:{d:Int | ((d >= 1) && (d <= 100))}) ->  Float { 
    native "lambda x: lambda y: math.pow(x , y)"
    } 

