#import Math_abs from Math;

#@minimize_int(Math_abs(2024 - fun(253)))
#def fun (i:Int) : Int  {
#    (?hole: Int) * i
#}


def year : Int = 2023;
@minimize_int( year - synth(7))
def synth (i:Int): Int { (?hole: Int) * i}
