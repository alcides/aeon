# import ..prelude.J

###
funcao : (x:{Integer where x > 0}, y:Integer) -> z:{Integer where z>0 and z <= 0} {
    x * 1
    x + y
    print(1)
}
###

funcao2 : (x:Integer, y:{Double where y > 0 and z <0}) -> d:Integer where [x > 0]{
    1 + 1
    (2 * f(1,2))
    asd : Integer = x
}
