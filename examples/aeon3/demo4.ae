decipher : (x:Integer) -> out:Integer {
    (x - 10);
}

cipher : ({x:Integer | x > 0}, key:Integer) -> z:Integer where {z > 0 and x == decipher(z)} {
    // x + key;
    [Integer];
}

//outraFuncaoComBuraco : () -> z:Integer {
//    [(x:Integer -> Integer)]([Integer]);
//}