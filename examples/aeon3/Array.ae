type T => Array<T> (size:(Integer where self >= 0)) as Array {

    range(min:Integer, max:Integer) -> array:Array<Integer> where ((min <= max) and res.size == (max - min)) = native;

    reduce(array:Array<T>, f:(T -> (T -> T)) -> T) -> _:T where (array.size > 0)
}
