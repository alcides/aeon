import ..prelude.J

# Problema: Doubles nao funcionam ainda

id : (i:Double) -> j:Double {
   i
}

sum : (i:Integer, j:Double) -> a:Double where [(id(a) - (i + j)) < 0.0001] {
   (i+j)
}


v : (i:Integer, k:Double) -> j:Double where [(id(j) - (i + k)) < 0.0001] {
   …
   # sum(i, k)
}


main : (_:Array<String>) -> _:Void {

    a : Integer = 1
    b :Double = 2.0

    J.out(v(a, b))
}
