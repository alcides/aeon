import aeon/libraries/image;

monalisa : Image = load_image("aeon/examples/aeon/mona.jpg");

generate_mona() -> {img : Image | img.width == monalisa.width && img.height == monalisa.height and
                                  @minimze(image_diff(monalisa, img))} {
    ??;
}