# Benchmark: synthesize a negative integer in [-50, -10].
# Tests constraint satisfaction when the valid region is entirely negative.

@minimize_int(0)
def result : {v:Int | v >= 0-50 && v <= 0-10} = ?hole;
