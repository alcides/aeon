# Benchmark: synthesize a function body for clamp(x:Int) : {y:Int | y >= 0 && y <= 100}.
# Any expression whose value provably lies in [0, 100] satisfies the type.
# A constant like 50 is the simplest valid solution.

@minimize_int(0)
def clamp (x:Int) : = y:Int | y >= 0 && y <= 100 {
    ?hole
}
