# Benchmark: synthesize an integer in the closed range [10, 20].
# Tests basic integer constraint satisfaction over a small contiguous interval.

@minimize_int(0)
def result : {v:Int | v >= 10 && v <= 20} = ?hole;
