type Tuple;

def Tuple_new : forall a:B, forall b:*, (e1:a) -> (e2:b) -> Tuple = native "lambda x: lambda y: (x,y)";
def Tuple_fst : forall a:*, (t:Tuple) -> a = native "lambda x: x[0]";
def Tuple_snd : forall b:*, (t:Tuple) -> b = native "lambda x: x[1]";
