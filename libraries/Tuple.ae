type Tuple;

def Tuple_new : (e1:Top) -> (e2:Top) -> Tuple = native "lambda x: lambda y: (x,y)";
def Tuple_fst : (t:Tuple) -> Bottom = native "lambda x: x[0]";
def Tuple_snd : (t:Tuple) -> Bottom = native "lambda x: x[1]";
