print<T>: (a:T) -> {b:T | a == b} = native;

abs<T>: (a:T) -> {b:T | b == (a >= 0 ? a : -a)} = native;

id<T>: (a:T) -> {b:T | a == b} = native;

forall<T>: (a:List<T>, f:(T -> Boolean)) -> c:Boolean = native;  // TODO:

exists<T>: (a:List<T>, f:(T -> Boolean)) -> Boolean = native;    // TODO:

fix<T>: (x:(T -> T)) -> T = native;

match_csv: (path:String) -> Boolean = native;
