// import outroPacote;
import teste2 from outroPacote;

type Xato as String;

define printString : (a:String, b:String) -> x:String {
    print(a);
    print(b);
}

define operationIntegers : (y:Integer, {x:Integer | x > 10}) -> z:Integer where {z > 10 and 10 > 3} {
    x * y;
}

define print3 : () -> x:String {
    printString("Ola", "Joao");
}

define main : () -> x:String {

    print3();
    print(operationIntegers(32, 3));
    printString(2, 3);


    x : Integer = 10;
    print(x);

    y : String = "Ola Paulo";
    print(y);
}
