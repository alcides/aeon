inductive IntList
| empty : IntList
| cons (hd:Int) (tl:IntList) : IntList

def mk : IntList = (.cons 1 (.empty) : IntList);

def head_or_zero (l:IntList) : Int =
    match l with
    | .empty => 0
    | .cons hd tl => hd;

def main (args:Int) : Int = head_or_zero mk;
